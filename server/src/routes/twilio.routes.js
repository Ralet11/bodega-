import express from 'express';
/* import { makeCall, voiceResponse, handleKeyPress } from '../controller/twilio.controller.js';

const router = express.Router();

router.post('/make-call', makeCall);
router.post('/voice', voiceResponse);
router.post('/handle-key', handleKeyPress); */

export default router;